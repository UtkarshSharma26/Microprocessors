DATA SEGMENT
ARR DB 5H,7H,8H
DATA ENDS
CODE SEGMENT
ASSUME CS:CODE,DS:DATA
START: MOV AX,DATA
       MOV DS,AX
       MOV CL,06H
       

LP1: MOV BX,CX
     LEA SI,ARR

LP2: MOV AL,[SI]
     INC SI
     CMP AL,[SI]
     JB LP3
     XCHG [SI],AL
     MOV [SI-1],AL

LP3: DEC BX
     JNZ LP2
     LOOP LP1

     MOV AH,4CH
     INT 21H
CODE ENDS
END START
